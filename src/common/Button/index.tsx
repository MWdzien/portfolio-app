import styles from "./styles.module.scss"
import React from "react";

const Button: React.FC = (children, backgroundColor="var(--background-color)") => {


    return (
        <div className={styles.button}>

        </div>
    )
}

export default Button